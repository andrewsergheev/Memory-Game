<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sirghi.andrei.memoryofagoldfish">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MemoryOfAGoldfish">
        <activity android:name=".HighScoreActivity"></activity>
        <activity android:name=".HighScoreSequence" />
        <activity android:name=".HighScoreTurns" />
        <activity android:name=".About" />
        <activity android:name=".HighScoreTime" />
        <activity android:name=".GameActivity" />
        <activity android:name=".List" />
        <activity android:name=".PuzzleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>